#ifndef TABLE_H
#include "binarysearchtree.h"
#include "student.h"
#include "faculty.h"
#include <fstream>
#include <string>


template <typename RowType>
class Table{
private:
  string tableName;
  BinarySearchTree tableDataStructure;

public:
  Table(String table){
    tableName = table;
    ifstream fin(tableName);
if (!fin){
  ofStream file;
  file.open(fout(tableName))
}

};

///read from file into a BST





  }
  serach(){}
  add()
  remove()
  update()

};






#endif
